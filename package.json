{
  "name": "flowpilot-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "FlowPilot — TypeScript framework for production-grade conversational chatbots",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
